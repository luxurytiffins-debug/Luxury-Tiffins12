import { db } from "@/lib/db";
export default async function Admin(){const [orders,users,products]=await Promise.all([db.order.count(),db.user.count(),db.product.count()]);return <main className="container-x py-16"><p className="gold text-xs uppercase tracking-[0.3em]">Admin</p><h1 className="mt-3 text-4xl font-bold">Dashboard</h1><div className="mt-8 grid gap-4 md:grid-cols-3">{[["Orders",orders],["Customers",users],["Products",products]].map(x=><div className="card p-6" key={x[0]}><p className="text-sm text-white/50">{x[0]}</p><div className="mt-2 text-3xl font-bold">{x[1]}</div></div>)}</div></main>}